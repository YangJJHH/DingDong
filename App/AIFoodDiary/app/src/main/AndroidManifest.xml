<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.aifooddiaryapp">

    <!-- 인터넷 권한 선언 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIFoodDiaryApp">
        <activity android:name=".HomeActivity">

        </activity>
        <activity android:name=".Register_id_pass"> </activity>
        <activity android:name=".Register_sex"></activity>
        <activity android:name=".Register_age"></activity>
        <activity android:name=".Register_name" />
        <activity android:name=".Register_body" />
        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
    </application>

</manifest>